class Interface:

    def __init__(self):
        self.inputData = {}
        return 

    def input(keys):
        for key in keys:
            value = input(key + "を入力してください。")
            inputData[key] = value
        
        self.inputData = inputData
        return 